// 2012 Nordic Collegiate Programming Contest (NCPC)
// 100112A

#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	string a, b;
	cin >> a >> b;
	cout << (b.size() <= a.size() ? "go" : "no") << "\n";
}
